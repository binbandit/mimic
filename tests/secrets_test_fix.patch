--- a/tests/secrets_test.rs
+++ b/tests/secrets_test.rs
@@ -75,6 +75,7 @@ mod secrets_tests {
 
     #[test]
     #[cfg(target_os = "macos")]
+    #[ignore]
     fn test_secret_exists() {
         cleanup_test_secret();
 
@@ -91,6 +92,7 @@ mod secrets_tests {
 
     #[test]
     #[cfg(target_os = "macos")]
+    #[ignore]
     fn test_list_secrets() {
         cleanup_test_secret();
 
@@ -115,6 +117,7 @@ mod secrets_tests {
 
     #[test]
     #[cfg(target_os = "macos")]
+    #[ignore]
     fn test_remove_secret() {
         cleanup_test_secret();
 
@@ -156,6 +159,7 @@ mod secrets_tests {
 
     #[test]
     #[cfg(target_os = "macos")]
+    #[ignore]
     fn test_get_all_secrets() {
         cleanup_test_secret();
 
@@ -173,6 +177,7 @@ mod secrets_tests {
 
     #[test]
     #[cfg(target_os = "macos")]
+    #[ignore]
     fn test_secret_with_special_characters() {
         cleanup_test_secret();
 
